产生规范协议族

广度优先遍历//有向图的遍历
如果队列为空，则遍历完成

0.点儿是否到达末尾，是则弹出
1.否则产生新节点[1]
2.check一下[2]， 新生节点是否已经被遍歷
如果是则不添加，否则添加到队尾,（ ？并分配一个int 型的值，？）進入queue的nod ，存到vector中
3.新节点check完（添加或不添加檢查完），老節點弹出

[1]如何新生
[2]如何check:用新生节点的任一rule, 包括节点信息， 左id，右id. 从已有的状态中搜索， 若匹配，则不添加
已有的状态

------------------------------------------------------------------------
根据协议族造表
